import { SearchBar } from "@/components/searchbar";

export default function Page() {
    return (
        <main className="items-center p-10 bg-black text-white font-[family-name:var(--font-geist-sans)]">
            <SearchBar />
        </main>
    )
}